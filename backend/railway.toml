[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo '=== START ===' && npx prisma migrate deploy && echo '=== PRISMA DONE ===' && node dist/main 2>&1; echo '=== NODE EXITED $? ==='"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
