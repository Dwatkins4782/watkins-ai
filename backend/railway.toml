[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo START && node dist/main 2>&1; echo EXIT_CODE=$?"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
