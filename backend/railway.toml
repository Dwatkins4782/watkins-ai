[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node -e \"console.log('HELLO'); console.log(require('fs').readdirSync('dist').join(',')); console.log('DONE')\" ; sleep 300"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
