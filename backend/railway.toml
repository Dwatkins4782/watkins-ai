[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "echo '=== START ===' && ls -la dist/main.js && npx prisma migrate deploy && echo '=== LAUNCHING NODE ===' && node dist/main 2>&1"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
