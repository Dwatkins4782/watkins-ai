[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "find /app -maxdepth 2 -type f -name '*.js' | head -20 && sleep 60"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
