[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx prisma migrate deploy && echo '=== PRISMA DONE, STARTING NODE ===' && exec node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
